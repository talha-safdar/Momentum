#include "Task.h"

Task::Task(const wxString& title, const wxString& dexcription, TaskStatus status) : title(title), description(description), status(status) {
				// add initialisation logic if necessary
}